export const reload = forcedReload => window.location.reload(forcedReload);
export const assign = DOMString => window.location.assign(DOMString);
export const replace = DOMString => window.location.replace(DOMString);
