/* eslint import/prefer-default-export:"off" */
import PT from 'prop-types';

const SysselsettingPropType = PT.shape({
  SysselsettingType: PT.string,
});

export { SysselsettingPropType as Sysselsetting };

